# Development Dockerfile - supports hot reload
# Uses Vite dev server instead of production build

FROM oven/bun:1

WORKDIR /app

# Install dependencies (copied to avoid volume mounting issues)
COPY package.json bun.lock /app/
COPY frontend/package.json /app/frontend/

WORKDIR /app/frontend

RUN bun install

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server with hot reload
# Note: source code will be mounted as volume in compose file
CMD ["bun", "run", "dev"]
